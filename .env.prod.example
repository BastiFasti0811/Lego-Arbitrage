# Copy to .env.prod on the Hetzner server and keep the real values out of git.
# Example deploy:
# docker compose --env-file .env.prod -f docker-compose.prod.yml up -d --build

COMPOSE_PROJECT_NAME=lego-arbitrage
DATA_ROOT=/mnt/HC_Volume_105179687/lego-arbitrage

POSTGRES_DB=lego_arbitrage
POSTGRES_USER=lego
POSTGRES_PASSWORD=CHANGE_ME_DB_PASSWORD

# Use the IP while no domain exists. Switch to the real domain later to let
# Caddy obtain HTTPS certificates automatically.
CADDY_SITE_ADDRESS=http://178.104.97.121
# CADDY_SITE_ADDRESS=lego.example.com
